<% provide(:title, "#{@article.title} Edit") %>

<%= form_for @article, url: {action: 'update'}  do |f| %>
  <%= render 'shared/error_messages', f: f %>
  <%= render 'form', f: f, action_name: "Save" %>
<% end %>

